DECLARE 
V_NAME VARCHAR2(30);
CURSOR CUR_VAR(VAR_e_ID NUMBER := 50) IS SELECT NAME FROM EMPLOYEE WHERE SALARY > VAR_e_ID;
BEGIN 
OPEN CUR_VAR;
LOOP
FETCH CUR_VAR INTO V_NAME;

EXIT WHEN CUR_VAR%NOTFOUND;
DBMS_OUTPUT.PUT_LINE(V_NAME);
END LOOP;
CLOSE CUR_VAR;
END;
