DECLARE 
V_NAME VARCHAR2(30);
CURSOR CUR_VAR IS SELECT NAME FROM EMPLOYEE WHERE SALARY >100;
BEGIN 
OPEN CUR_VAR;
LOOP
FETCH CUR_VAR INTO V_NAME;
DBMS_OUTPUT.PUT_LINE(V_NAME);
EXIT WHEN CUR_VAR%NOTFOUND;
END LOOP;
CLOSE CUR_VAR;
END;
